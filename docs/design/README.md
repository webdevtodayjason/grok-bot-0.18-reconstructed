# Design sources

- `titanium-bot-landing.draft.html`: the titanium.bot landing page draft (2026-09-06), two directions judged into one
  (`landing-judgement.md`), then re-skinned to the Titanium Computing brand system from `~/code/TC3.0`. Published at
  https://artifacts.semfreak.dev/a/titanium-bot/landing-tc-7d1fe3df/ (republish with project `titanium-bot`, slug `landing-tc-7d1fe3df`).
- `system-map.html`: the whole system as measured on the R750 that day, planned pieces dashed. Published at
  https://artifacts.semfreak.dev/a/titanium-bot/system-map-tc-997b4d9b/ (slug `system-map-tc-997b4d9b`).

Both are single files with no external requests except the site's own Google Fonts link. No em dashes.
