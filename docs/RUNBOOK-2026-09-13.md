# Runbook 2026-09-13: today's board

Jason, 07:47 CDT: build it, use Codex and Grok where they fit to keep the token cost down.
Order is priority; hours are worker wall-clock, not Jason's.

| # | item | who | hours | ship |
|---|---|---|---|---|
| 1 | Build 21 call: trim, earpiece, short answers, filed note, ended card | Jason | 0.1 | none; the relay settle line and TestFlight feedback are the measurement |
| 2 | BOOT-1: the console draws before the connectors answer (hydrate awaits every listMcpServerTools first) | Claude implementer (console adapter, browser leg) | 3 | relay |
| 3 | DISCOVER-1: welcome bar, six measured steps, per-person hide | Claude (relay evidence read + cp hide) and Codex (console bar + dropdown + browser leg) | 4 | relay + cp |
| 4 | FEEDBACK-3: TestFlight feedback pulled hourly into the Feedback panel, Titan pinged on every new row of either kind | Grok via Orca (the cp poller and panel column, modelled on cp/support.mjs); Claude places the ASC key as a cp secret | 3 | cp |
| 5 | ONBOARD-4 press: a Delete button instead of the timer, if Jason says "press" | Codex (cp/kept.mjs + button) | 0.5 | cp |
| 6 | VOICE-17: the box holds the realtime session, no hand-off | Claude, two implementers (host session + relay pipe) | 16 | host + relay + app |
| 7 | VOICE-2: voice on included minutes through the proxy | Claude | 4 | cp + relay |
| 8 | VOICE-5 + SPEND-1: caps and usage in money | Grok research pass on both vendors' meters, then Claude | 6 | cp + console |
| 9 | Feedback sweep: the 22 older rows closed or filed | Grok via Orca | 2 | none |
| 10 | COST-4, DASH-7, GATE-14/15 | Codex | 7 | relay |

Jason only, minutes each: APNs push key, GitHub token for the Feedback panel, notarisation cert,
the three farm steps, close the five support test rows.

Rules for today: every ship measured on the R750 and logged in ORCHESTRATION.md; relay restarts
only between calls (Jason is testing voice today, check `voice_sessions` first); host updates on
idle boxes; testers get a WHAT-TO-TEST note with every build.
